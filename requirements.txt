fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
psutil>=5.9
# 按实际引擎安装其重量级依赖：torch、transformers、vllm、peft。
# GGUF（可选，CUDA 构建请按本机 CUDA/编译器选择对应 wheel）：llama-cpp-python
# safetensors（HF safetensors 权重支持，通常会随 transformers 安装）：safetensors
